-- Migrate from v1 (FavoriteQuote only) to v2 (adds QuoteOfTheDay cache).
CREATE TABLE QuoteOfTheDay (
    qotdDate TEXT NOT NULL PRIMARY KEY,
    id INTEGER NOT NULL,
    body TEXT NOT NULL,
    author TEXT NOT NULL,
    favoritesCount INTEGER NOT NULL DEFAULT 0
);
